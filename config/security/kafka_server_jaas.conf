// Broker JAAS for SASL/SCRAM inter-broker and controller communication.
// Must match the admin SCRAM user created at storage format time.
KafkaServer {
  org.apache.kafka.common.security.scram.ScramLoginModule required
  username="admin"
  password="admin-secret";
};
